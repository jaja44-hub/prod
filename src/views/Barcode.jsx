import React from 'react';

export default function Barcode() {
  return (
    <div>
      <h2>Barcode / QR</h2>
      <p style={{ color: 'var(--text-muted)' }}>Barcode labels and scanning utilities (mock).</p>
    </div>
  );
}
