import React from 'react';

export default function Payroll() {
  return (
    <div>
      <h2>Payroll</h2>
      <p style={{ color: 'var(--text-muted)' }}>Payroll processing (mock).</p>
    </div>
  );
}
